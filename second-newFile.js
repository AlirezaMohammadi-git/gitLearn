

console.log("this is second file!")


async function goodFunction() {
    // console.log("this function is causing some errors!");
    console.log("but we commit it befor and now we don't know this function is the reson of error!");
    console.log(`so we just use "git reset commit-hash" to see all changes appeared after that commit hash and fix theme all!`);
    console.log(`you can grab commit-hash from "git log"`);
}



// async function badFunction() {
//     console.log("this function is causing some errors!");
//     console.log("but we commit it befor and now we don't know this function is the reson of error!");
//     console.log(`so we just use "git reset commit-hash" to see all changes appeared after that commit hash and fix theme all!`);
//     console.log(`you can grab commit-hash from "git log"`);
// }